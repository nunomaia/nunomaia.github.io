---
layout: single
title:  Locate .NET Assembly for a given type in X++ 
tags:                                             
  - D365FO
  - Power Automate
categories:
 - Electronic Reporting
 - Invoice
 - Printing 
---

Organization administration > Document management > Document Types

 ![!](/assets/images/posts/2022/01/20220105_01.png)
 
  Organization administration > Electronic reporting > Electronic reporting destination

![!](/assets/images/posts/2022/01/20220105_02.png)

Accounts receivable > Setup > Forms > Form setup

![!](/assets/images/posts/2022/01/20220105_03.png)

![!](/assets/images/posts/2022/01/20220105_04.png)

![!](/assets/images/posts/2022/01/20220105_05.png)

Accounts receivable > Invoices > All free text invoices




````csharp
using Microsoft.WindowsAzure.Storage;
using Microsoft.WindowsAzure.Storage.Blob;
using Microsoft.WindowsAzure.Storage.File;

class Blog_ERDocuManagementBlob
{
    [SubscribesTo(classStr(ERDocuManagementEvents), 
        staticDelegateStr(ERDocuManagementEvents, attachingFile))]
    public static void ERDocuManagementEvents_attachingFile(ERDocuManagementAttachingFileEventArgs _args)
    {
        if (!_args.isHandled())
        {
            DocuType docuType = DocuType::find(_args.getDocuTypeId());

            if (docuType.FilePlace == DocuFilePlace::ExternalBlobStorage)
            {
                var stream = _args.getStream();
                if (stream.CanSeek)
                {
                    stream.Seek(0, System.IO.SeekOrigin::Begin);
                }

                CloudStorageAccount storageAccount = CloudStorageAccount::Parse("<Connection String>");
                CloudBlobClient blobClient = storageAccount.CreateCloudBlobClient();
                CloudBlobContainer blobContainer = blobClient.GetContainerReference("<Container>");
                CloudBlockBlob blockBlob = blobContainer.GetBlockBlobReference(_args.getAttachmentName());
                
                blockBlob.UploadFromStream(stream, null, null, null);

                _args.markAsHandled();
            }
        }
    }
}
````